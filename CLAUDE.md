# AGENTS.md
## 沟通规则

- **中文输出**：所有面向用户的回复、解释、总结、计划、提交信息说明等，均必须使用**中文**输出。

## 编码规则

- **中文注释**：编写的**业务逻辑代码必须包含完整的中文注释**，包括但不限于：
    - 类级别 Javadoc：说明类职责、领域角色
    - 方法级别 Javadoc：说明方法用途、参数含义、返回值、异常场景
    - 关键业务分支/复杂逻辑的行内注释：说明为什么这么做，而不仅是做了什么
    - 重要常量、枚举、字段的中文说明

- **编码规范**:
    - 不允许使用魔法值

## 文件编码与换行符规范

- IDE 中的文件换行符必须使用 Unix 格式，不得使用 Windows 格式。
- 项目文件统一使用 **UTF-8** 编码和 **LF（`\n`）** 换行符，禁止使用 **CRLF（`\r\n`）**。
- 新建或修改文件时必须保持 LF，禁止因换行符转换造成整文件无意义 diff。
- 在 Windows 环境执行文件写入、格式化或代码生成后，必须检查并确保换行符仍为 LF。
- 推荐在项目根目录通过 `.gitattributes` 强制文本文件使用 LF：

```gitattributes
* text=auto eol=lf
*.java text eol=lf
*.xml text eol=lf
*.yml text eol=lf
*.yaml text eol=lf
*.properties text eol=lf
*.md text eol=lf
```

## 基本原则

- 默认使用中文沟通，面向用户的回复、解释、计划、总结、提交说明均使用中文。
- 先理解现有实现、代码上下文和真实目标，再修改代码；不要一上来重构。
- 结果导向：完成任务、验证结果、修复发现的问题，不能留下明显半成品。
- 最小必要修改：只改完成任务必须修改的部分，不顺手重构无关代码、不制造大面积格式化 diff。
- 优先遵循项目现有风格，包括命名、包结构、日志、异常处理、测试风格和 SQL 风格。
- 不为了未来不确定需求提前设计，不为简单逻辑引入复杂模式。
- 不编造未验证结果、日志、调用链、性能收益或根因。

## 工作流程

- 开始前先查看相关文件、调用方、测试和文档，确认真实边界。
- 存在歧义时先判断是否影响结果；只有会影响实现正确性或系统风险时才向用户提问。
- 发现用户方案明显会引入长期风险、性能风险、安全风险或维护困难时，直接指出并说明替代方案。
- 修改完成后必须自查：逻辑、边界条件、副作用、日志、注释、测试和项目风格。
- 如果无法运行验证，必须在最终说明中明确原因和缺失证据。

## 编码规范

- 业务逻辑代码必须有中文注释，但注释要解释业务规则、设计原因和边界，不要给显然 setter/getter 或直白赋值堆噪声。
- 类级 Javadoc 说明类职责、所属领域、输入输出和关键协作对象。
- 方法级 Javadoc 说明方法用途、参数含义、返回值和异常场景；复杂方法补充逻辑说明。
- 关键业务分支、协议边界、兼容映射、fallback、feature-toggle、预处理归一化等非显然逻辑必须用行内中文注释说明“为什么这么做”。
- 重要常量、枚举、字段必须有中文说明；不允许裸写魔法值。
- 命名遵循 Java 常规与项目约定：类名 UpperCamelCase，方法和变量 lowerCamelCase，包名小写。
- DTO/VO/请求响应命名遵循现有约定：`*Req`、`*Rsp`、`*DTO`、`*VO`、`*Cmd`、`*Query`。
- 不吞异常，不泄露敏感信息，不手工输出手机号、密码、token 等敏感数据。
- 不使用 `System.out` / `System.err`，启动类极特殊场景除外。

默认可使用 subagent 做探索、分析、审查和发现问题；subagent 应优先报告发现，不直接改代码。

subagent 只有在以下条件都满足时才可直接修改代码：

- 任务边界清晰。
- 所属模块或目录边界清晰。
- 修改范围局部。
- 可独立验证。

架构、业务逻辑、跨模块重构、高影响改动必须由 subagent 给出建议，主 agent 负责最终决策、集成和验证。避免多个 subagent 修改同一批文件。

代码审查、安全审查、性能审查、Bug 排查和架构审查时，subagent 输出必须包含：

- 文件路径。
- 严重级别。
- 根因。
- 推荐修复方式。

主 agent 必须汇总 findings，并负责最终实现与验证。

## 最终回复要求

- 结论优先，说明改了什么、为什么、影响范围、风险点和如何验证。
- 输出简洁清晰，不写空话，不重复用户输入。
- 如果发现无关 bug 或历史问题，可以指出，但不要擅自修改。
